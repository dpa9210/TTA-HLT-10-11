#RandomXTable

number = int(input("Enter a number to multiply: "))
multiplier = 1
for randomNumLoop in range(multiplier*number, 13, number):
  print (number, "x", multiplier, "=", randomNumLoop)
  multiplier=multiplier+1
